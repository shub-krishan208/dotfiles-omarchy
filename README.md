# Omarchy Dotfiles Backup

This repo contains all the Omarchy Dotfiles I've setupa nd will help in case of updates and needing backup.

- currently i've changes some configs in ash theme, waybar, hypr (obviously).

## TO-DO:

[ ] make notification history setup
[X] make a clipboard history setup with support for images
[X] add support for qt based applications
